import { ArrowUpRight, Leaf, Utensils } from "lucide-react";
export default function RecipeCard({ food, onOpen }) { return <article className="recipe-card"><div className="recipe-art"><span>{food?.name?.slice(0,1)?.toUpperCase() || "F"}</span><i><Leaf size={15}/></i></div><div className="recipe-copy"><span className="food-kicker"><Utensils size={13}/> {String(food?.mealType || "INDIAN").replaceAll("_"," ")}</span><h3>{food?.name || "Indian dish"}</h3><p>{food?.cuisine || "Indian cuisine"} · {food?.servingSize ?? "—"} {food?.servingUnit || "serving"}</p><div className="recipe-meta"><span>{food?.calories ?? "—"} kcal</span><span>{food?.protein ?? "—"} g protein</span></div></div><button className="card-arrow" onClick={() => onOpen?.(food)} aria-label={`View ${food?.name || "food"}`}><ArrowUpRight size={17}/></button></article>; }
