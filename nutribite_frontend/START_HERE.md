# NutriBiteAI Frontend — fixed

## Run
PowerShell:
```powershell
cd "<this-folder>"
npm.cmd install
npm.cmd run dev
```

Open:
http://localhost:5173/home

Backend:
http://localhost:8080

The frontend now supports `/home` as the dashboard route.
